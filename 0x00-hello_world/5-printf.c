#include <stdio.h>

/**
 * main is entry point
 * Descripation for fn: prints "with proper grammar, but the outcome is a piece of art," and put end line
 * return 0 end of program
 */

int main(void)
{
        printf("%s","with proper grammar, but the outcome is a piece of art,\n");
        return(0);
}
